# Core dependencies (aligned with pyproject.toml [project.dependencies])
more_itertools>=10.0.0
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0

# Interdependent packages
filewise>=3.11.6
paramlib>=3.4.10

# Optional: NetCDF / climarraykit features in time_handling (pip install 'pygenutils[climate]')
# climarraykit>=0.1.0

# Other optional extras (see pyproject.toml)
# pip install 'pygenutils[xarray]'
# pip install 'pygenutils[arrow]'
# xarray>=2024.2.0
# arrow>=1.2.0
